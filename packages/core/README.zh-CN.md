# @acme-ai/core

`@acme-ai/core` 提供所有 Acme 包和应用程序使用的基础类型、接口和工具函数。它定义了核心概念的
数据结构，包括 Vault、Project、Thread、Agent 和 Message。

**核心导出：**
- 核心数据类型 (Vault, Project, Thread, Message)
- Agent 配置接口
- 权限和安全类型
- 通用工具函数

**注意：** 此包没有外部运行时依赖，确保最小化的包体积影响。
