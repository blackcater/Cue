{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:desktop": "bun run --filter='@acme-app/desktop' dev",
    "dev:ios": "bun run --filter='@acme-app/mobile' ios"
  },
  "devDependencies": {
    "@types/node": "22",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0"
  }
}
